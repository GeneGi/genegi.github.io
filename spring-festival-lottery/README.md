# 春晚庙会抽奖网站 (Spring Festival Lottery)

一个用于春节庙会活动的在线抽奖系统。

## 功能特性

- 🎁 奖品配置管理
- 🎲 随机抽奖功能
- 🎊 春节主题界面
- 💾 本地数据持久化
- 🔄 系统重置功能

## 技术栈

- **前端框架**: React 19
- **语言**: TypeScript
- **构建工具**: Vite
- **测试框架**: Vitest + fast-check
- **数据存储**: Firebase Firestore + localStorage (备份)

## 项目结构

```
src/
├── components/     # React 组件
├── services/       # 业务逻辑服务
├── types/          # TypeScript 类型定义
├── utils/          # 工具函数
├── App.tsx         # 主应用组件
└── main.tsx        # 应用入口
```

## 开发指南

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 运行测试

```bash
# 运行所有测试
npm test

# 运行测试并显示 UI
npm run test:ui

# 运行测试一次（CI 模式）
npm run test:run
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 测试策略

项目采用双重测试方法：

1. **单元测试**: 验证特定示例、边界情况和错误条件
2. **基于属性的测试**: 使用 fast-check 验证通用属性在所有输入下都成立

## 许可证

MIT
